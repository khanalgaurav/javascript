/*
Constant is used to permanantly assign value.
They cannot be changed after assigned once.
*/
const piValue=3.14

/*
let is used to define a variable in js.
var is also used to define a variable in js but it causes issues in block scope and functional scope.
"So we use let to define variable". 
*/
let firstName="Gaurav"
var lastName="Khanal"
// we can also create variables without declaring its type but this is dumb so dont do it.
addressCity="Kapan"